import {colors, font_size} from "../../constants/theme.js";

export const styles = {
    btn: {
        width: '100%',
        backgroundColor: colors.blue,
        padding: 12,
        borderRadius: 6
    },

    text: {
        color: '#fff',
        fontSize: font_size.md,
        textAlign: 'center'
    }
}